# Policies (PCI DSS)

Store PCI-specific governance policies here if you maintain PCI overlays separate from your general security policy set.

If you rely on an existing “business” policy library (for example, `business/*.pdf`), reference those documents from `controls/pci-dss-requirements.md` and store only PCI-specific deltas/overlays in this folder.

